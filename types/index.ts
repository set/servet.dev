export interface Project {
  title: string
  description: string
}

export interface BlogPost {
  title: string
  date: string
  excerpt: string
  slug: string
}

export interface Logo {
  url: string
  alt: string
}